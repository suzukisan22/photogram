<div class="col-md-8 col-md-offset-2 col-sm-10 col-sm-offset-1">
  <% @users.each do |user| %>
    <div class="pic-area">
      <p class="pic-user-info">
        <%= link_to user_profile_path(user.username), class: 'username' do %>
          <span><%= profile_img(user) %></span>
          <strong><%= user.username %></strong>
          <%= user.name %>
        <% end %>
      </p>
        <% unless user.secret == 0 %>
          <div class="nothing text-center">
            <h3>このアカウントは非公開です</h3>
            <p>このアカウントの写真や動画を見るには、フォローリクエストを送信してください。</p>
          </div>
        <% else %>
          <div class="row">
            <% user.pictures.limit(3).each do |picture| %>
              <div class="col-sm-4">
                <%= image_tag("#{picture.avatar.url}") %>
              </div>
            <% end %>
          </div>
        <% end %>
    </div>
  <% end %>
</div>
